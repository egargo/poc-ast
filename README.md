A proof of concept to showcase how to structure an Abstract Syntax Tree (AST)
for my (toy) programming language project, assuming that input were tokenized
and is being parsed with Recursive Descent Parser (RDP).

Nothing serious, just a hobby project.
